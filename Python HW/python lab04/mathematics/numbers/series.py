
def sum(*, list=[]):
    """Return the sum of the elements in the list."""
    sum = 0
    for i in list:
        sum += i
    return sum

def average(*, list=[]):
    """Return the average of the elements in the list."""
    sum = 0
    for i in list:
        sum += i

    return sum / len(list)

